import { baseContracts } from "@cobuild/wire";

export {
  BASE_CHAIN_ID,
  COBUILD_PROJECT_ID,
  COBUILD_PROJECT_ID_BIGINT,
  WETH_ADDRESS,
} from "@cobuild/wire";

// Default ETH price fallback (USD)
export const DEFAULT_ETH_PRICE_USDC = 3000;

export const contracts = baseContracts;
